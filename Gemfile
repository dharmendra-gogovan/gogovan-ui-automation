# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}"}

gem 'appium_lib'
gem 'rspec'
gem 'rspec-expectations'
gem 'spec'
gem 'test-unit' # required for bundle exec ruby xunit_android.rb
